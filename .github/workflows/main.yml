on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        #uses: rscohn2/setup-oneapi@v1.1
        uses: ./
        with:
          components: |
            tbb
            dpl
